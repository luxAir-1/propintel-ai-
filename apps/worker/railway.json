{
  "$schema": "https://railway.app/railroad.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --no-frozen-lockfile && pnpm run build --filter=@propintel/worker"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "pnpm run start --filter=@propintel/worker",
    "restartPolicyMaxRetries": 5
  }
}
